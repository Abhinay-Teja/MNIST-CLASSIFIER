# Handwritten Digit Classification with PyTorch

This project implements a 3-layer feedforward neural network using PyTorch to classify handwritten digits from the *MNIST dataset, achieving **97.5% accuracy* on the test set.
